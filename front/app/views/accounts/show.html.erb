<h1 class="account_title">Account: <%= @account.name %></h1>

<%= link_to "Add Transactions", new_account_transactions_path(@account), class: 'btn btn-primary btn-large new_transactions' %>

<table class="table table-striped table-condensed transactions">
	<tr>
		<th class="creation">Creation</th>
		<th class="name">Name</th>
		<th class="amount">Amount</th>
		<th class="actions"></th>
	</tr>
	<% @account.transactions.each do |transaction| %>
		<%= render 'transactions/transaction', transaction: transaction %>
	<% end %>
</table>